# Railway build (Nixpacks): system packages for ffmpeg and canvas native deps.

[phases.setup]
aptPkgs = [
  "ffmpeg",
  "build-essential",
  "libcairo2-dev",
  "libpango1.0-dev",
  "libjpeg-dev",
  "libgif-dev",
  "librsvg2-dev",
]
